# Simple TLS config file
# "<ws>" in the port line will be replaced with a generated port
# and will be retrievable via NatsTestServer.getPort("ws")

port: 0

net: localhost

ws {
  port: <ws>
  no_tls: true
}